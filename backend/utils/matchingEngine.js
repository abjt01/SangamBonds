class MatchingEngine {
  static async processOrder(orderData) {
    // Simplified order processing for now
    return {
      success: true,
      order: orderData
    };
  }
}

module.exports = MatchingEngine;
