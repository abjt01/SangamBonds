import React from 'react';
import { Box, Typography } from '@mui/material';

const Trading = () => {
  return (
    <Box p={3}>
      <Typography variant="h4" gutterBottom>
        Trading
      </Typography>
      <Typography variant="body1">
        Trading interface will be here.
      </Typography>
    </Box>
  );
};

export default Trading;
